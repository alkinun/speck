# Generated paper tables

Final paper tables are generated from checked result records. Use stable IDs (`T01`, `T02`, …), retain
machine-readable CSV or JSON beside manuscript formatting, and emit a source manifest linking exact
inputs, analysis revision, command, output hash, and claim IDs.

Do not manually copy benchmark, parameter, token, FLOP, state, latency, throughput, or energy values
into the manuscript.
