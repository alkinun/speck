# Stack-Edu code stock: real-prefix recovery qualification

The [bound stock plan](../flagship/stack_edu_stock_preparation_v1.json) now consumes the
[completed eleven-language metadata](../../results/data/stack-edu-metadata-acquisition-20260914.json).
It preserves the qualified file-license, score, encoding, size, English-prose and security rules.
The [code-language policy](../flagship/CODE_LANGUAGES.md) explicitly records the corpus-scale
repository-cap decision, common vendor-path rule, missing upstream vendor/fork/commit ancestry,
fixed candidate allowance, and independent per-language final capacity gates.

At frozen implementation `a01fa02`, the
[real-prefix qualification](../../results/systems/stack-edu-stock-prefix-20260914.json) exercised
the first 512 C metadata rows with real SWH requests, retained payload/attempt receipts, content
identity, actual filtering/Gitleaks, Mistral counting, injected interruption after a durable
checkpoint, clean replay and completed-unit reopening. Both published output hashes match;
all output/security/fetch-batch receipts were checked again after completion.

The prefix retained **12 documents / 8,994 tokens**. Removals were 373 score failures,
124 license failures, two declared-length mismatches, and one non-English-prose failure.
No Gitleaks removal occurred in this prefix. These are small-prefix acquisition diagnostics,
not full source capacity, full exclusion, or a forecast of final language yields. Replays
share cached fetch inputs and are not independent network-performance measurements.

The full stock execution remains subject to complete-file exhaustion and post-exclusion
headroom. Every language must meet its target; surplus elsewhere cannot substitute. Sparse
eligible metadata or unavailable blobs require explicit follow-up, not relaxed filters.
The job preserves original row order, missing responses, failed transient attempts, uncommitted
tails and scanner attempts. Exhausted transient retries stop without silently skipping rows.
Source metadata/code blobs and these internal text outputs do not authorize model training.

Validation before qualification: **1,127 passed, 10 skipped, 125 deselected**, plus format,
lint, research catalog, archive, source-pin and diff checks. Fixtures cover corrupt/oversized
SWH payloads, missing/transient distinction, checkpoint corruption, exact resumed-output parity,
whole-document stopping, prefetch receipts, tokenizer counts and per-language capacity deficits.
