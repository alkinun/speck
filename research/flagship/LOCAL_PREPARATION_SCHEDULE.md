# Local preparation schedule — 2026-09-15

Follow the [active queue](PREPARATION_QUEUE.md) and contracts selected by the [catalog](../catalog.json).
The allocation is still under evaluation; no access date is confirmed. This schedule orders work by
readiness dependencies, not an assumed award date. The [previous schedule](../history/2026-09-15-readiness-cleanup/manifest.json)
is preserved with its original source-acquisition decisions and measurements.

| Priority | Local work | Completion evidence |
| --- | --- | --- |
| 1 | Reconcile already running finite source workers; avoid competing HDD exclusion | Published text/cache receipts, complete reopen checks, preserved failures and non-overlapping capacity accounting |
| 2 | Extend the [CPU-qualified bounded R0 executor](R0_EXECUTOR.md) with fresh-process recovery and numerical/cache checks | Reproducible input/configuration receipts, attempt recovery, timeout and GPU-hour accounting; local verification with GPU checks explicitly pending |
| 3 | Measure successor supply, scarce-source exposure and delivery/storage | New source-specific sheet for the selected broad base and study; actual eligible tokens distinguished from targets and estimates |
| 4 | Prepare coherent units and disjoint task pilots | Family/revision/window identities, qualified ordering/isolation, supported answers and counterfactual checks |
| 5 | Pin evaluation and complete the study freeze | Benchmark/scorer identities, family partitions, numerical guards, matched exposure, endpoints and complete costed run list |

Work on the executor, manifests and task/evaluation contracts can proceed while finite acquisitions
finish. Do not wait for all production data before preparing bounded R0 checks. Do not expand a source
because a retired E1/E3 quota remains in a worker plan. Any additional transfer needs a bound successor
that serves the current program and preserves existing source-use restrictions.

The [R0 shape result](R0_READINESS.md) establishes construction/accounting on the meta device, not GPU
fit or throughput. At the allocated site, qualify arm64 dependencies and KDA kernels, exact-shape
forward/backward/optimizer, numerical and cached-generation parity, four-GPU DDP, checkpoint/resume,
and startup/storage overhead within 70 GPU-hours. Site scheduler/requeue commands depend on actual
access details. Preserve failures and incomplete measurements; apply the registered length fallback.

R1 then uses measured costs and independent task pilots to freeze executable R2/R3 inputs and endpoints.
No full-budget model launch follows automatically from a local audit or completed download. The
1.2B flagship defaults to 320B base tokens; 400B is a measured-fit stretch within the unchanged B0 cap.
General-purpose quality and useful long-context results both remain to be demonstrated.
