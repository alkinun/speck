# Readiness for the long-context allocation

The owner approved the scope pivot. [plan_v4.json](plan_v4.json) is the selected design, with per-run
launch manifests still pending. Do not request the same strategic approval again.

## P0 -> R0

Prepare bounded qualified inputs, retained 1.2B shapes, storage/recovery commands, Slurm/site accounting
and software environment. R0 is explicitly budgeted hardware qualification and can precede the final
production corpus. It includes actual 4K/32K/128K fit/backward/resume and four-GPU throughput.

## R1 -> confirmatory study

Bind broad-source capacity/exposure and source-family partitions, verified standard/targeted tasks,
benchmarks/scorers, the three paired seeds, parent/branch identities, all processed/supervised token
endpoints, numerical quality floors and margins, failure rules, analysis and cost projections. Choose
R3 scale versus horizon route before any R2 confirmation outputs. Independent pre-results review must
resolve whether the exact paper question adds to related work. Implement and qualify study materializers
and analyzers; CPU fixtures alone do not establish real data capacity or GPU throughput.

## F0 -> base training

Verified production shards and tokenizer, actual site delivery, per-source reuse, total storage and
calendar budget, base token target and pre-decay checkpoints, optimizer/schedule and launch receipt.
Existing data-launch/firewall mechanics require compatibility qualification for the new partitions;
legacy D5/E2 audit names are retained unopened exclusions, not active mixture-selection decisions.

## Capability and release

Coherent-window sampler or equivalent preprocessing, measured long-unit yield, exact stage manifests,
separate-codebase post-training handoff, useful-length/general guardrails, pinned public/RAG baselines,
native/Transformers plus one accelerated serving backend, and final audit/artifact destinations.
CPU/GGUF is optional; no implicit context-parallel trainer or new RL system.

Current evidence and next actions belong to [status.json](../status.json). The transition preserves
old evidence and already recorded finite acquisition without interrupting jobs or expanding old quotas.
