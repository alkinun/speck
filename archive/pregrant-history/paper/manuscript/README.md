# Manuscript source

The venue and build toolchain are not selected yet. When they are frozen, keep the manuscript,
supplement, style files, and build entry point here.

The manuscript must reference claim IDs from `paper/claims.json` and generated outputs from
`paper/figures/` and `paper/tables/`. A clean build must validate claim status and source manifests
before producing the review PDF.
