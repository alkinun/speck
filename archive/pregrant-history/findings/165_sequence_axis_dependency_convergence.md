# 165 — Sequence research now has one acyclic activation order

The qualified convergence gate binds every current sequence successor into an eleven-node dependency
DAG rooted at the active finalist. No descendant can be registered or materialized before that root is
accepted through the frozen event chain.

The first descendant is the 24-cell mixer-by-position factorial: six accepted KDA/NoPE finalist cells
may be reused only after v3 acceptance, with 18 new cells. Its winner determines whether the existing
cache and ratio v2 designs are eligible. Both are specific to KDA/NoPE; another mixer or positional
treatment requires a newly derived and qualified parent-matched successor.

Cache representation and base refresh ratio remain separate conditional stages at the common exact-
attention scaffold. They are serialized on the single consumer GPU and must meet in a future paired
integration successor before recurrent-gate selection. If SiLU later replaces sigmoid, cache and ratio
effects require re-confirmation under that gate before compression work.

The convergence audit also removes a circular instruction from sequence-compression v2. Ratio v2
selects the base exact-attention refresh count. HCA/CSA studies use those slots as a fixed isolation
scaffold. Any final compressed-operator schedule/count check is a new downstream successor and cannot
reuse ratio-v2 output as evidence.

Known run counts are resource envelopes, never batch-launch authority: 12 active finalist, 18 new NoPE,
12+24 conditional cache, 15+18 conditional ratio, 6 or 12 recurrent-gate, and 24 three-axis discovery
runs. Only one stage may be registered at a time, every edge requires an exact parent hash, and each
future artifact freezes before its first output.

Pre-results sequence source/factorization work is now stopped. No parent is selected, no descendant
config exists, and implementation, training, and promotion remain false.

## Artifacts

- [Sequence dependency DAG](../research/paper-1/sequence_axis_convergence_v1.json)
- [Qualification](../results/Speck-Paper1/sequence-axis-convergence-qualified-v1.json)
