# 118 — HELMET archive acquired and hash-qualified

## Acquisition

The existing resumable partial completed through a transient one-shot service. Anonymous HTTPS curl
transferred the revision-pinned archive in 2,622.97 seconds. The final file is exactly 11,271,916,108
bytes and its independently recomputed SHA-256 is
`9d693981aa3c065b8b2ff82ddf946141cdc4ece4524f18bff6f3fbd2a86982d9`, matching the frozen contract.
No credential was exposed.

The archive resides on the expected ext4 UUID under a user-owned mode-0700 directory. After download,
5,633,159,593,984 bytes remain free versus the 68,719,476,736-byte acquisition floor. No superseded
partial was discarded.

## Boundary

Archive identity alone does not authorize extraction or HELMET execution. A streaming path/member
inventory must reject unsafe entries, verify every config-declared local path, and expose only small
license/README metadata for review. Fifty runtime-loaded entries plus component rights, gated tokenizer,
and model-judge blockers remain independent.

The completed acquisition removes the active-service conflict for finalist live qualification, but the
qualification must still be recorded from a clean v2 state before any launch.

## Artifact

- [HELMET archive acquisition](../results/Speck-Architecture-Promotion-v1/helmet-data-acquisition.json)
