# 123 — Fresh live gate for finalist control 0 attempt 2

## Qualified restart state

After the interrupted attempt and identical-rerun contract were registered, a new live gate revalidated
the program. The initial ledger now contains the pinned failed attempt and active attempt-2 contract,
with zero accepted results and the same dense pair-0 next cell.

All finalist units and checkpoint/result paths are absent. The RTX 3090 is idle at 45°C with 1 MiB used;
host memory is 23,536,611,328 bytes. The isolated ext4 volume retains the required mount options and
5,633,159,589,888 bytes free. HELMET remains inactive.

## Decision

The identical attempt-2 restart and its event successors are authorized. This is not automatic retry;
the interruption and rerun remain permanently visible. No quality branch, further retry, attribution,
promotion, novelty, release, or paper-scale authority is granted.

## Artifact

- [Fresh live qualification v3](../results/Speck-Paper1/finalist-launch-qualified-v3.json)
